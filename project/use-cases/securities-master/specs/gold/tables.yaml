tables:
- name: securities_master
  catalog: statestreet
  schema: g_statestreet
  description: "Gold wide table — Securities Master analytics mart. Grain: one row per active security version (is_current = TRUE)."
  columns: []
